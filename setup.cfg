[metadata]
name = api
version = 0.0.1

[options]
package_dir=
    =src
packages=find:
install_requires = 
    flask
    flask-cors
    flask-smorest
    marshmallow

[options.packages.find]
where=src

[options.extras_require]
dev =
    black
    isort
    pylint
